import React from 'react'

function Homepage() {
    return (
        <div>
            <h1>HOMEPAGE</h1>
        </div>   
    )
}

export default Homepage

//will contain links to the INDEX(ImageList) - /images