import React from 'react'

function Homepage() {
    return (
        <div>
            'homepage'
        </div>   
    )
}

export default Homepage

//will contain links to the INDEX(ImageList) - /images