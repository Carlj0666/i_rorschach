import React from 'react'

class CardsContainer extends React.Component {

    render () {
        return (
            <div>
                CardsContainer
            </div>
        )
    }
}

export default CardsContainer

/*This container needs to:
    1)  take in the INKBLOTS & COMMENTS from the rails api backend. 
    2) The INKBLOTS & COMMENTS will need to be passed to the IMAGE CARD - (Inkblot) child component & COMMENT CARD (Comment card) child components as PROPS
*/

